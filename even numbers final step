//program to determine the even number
#include <iostream>
using namespace std;
int main()
{
    int i,a,sum=0;
    cout<<"enter the number";
    cin>>a;
    for(i=0;i<=a;i+=2)
    sum=sum+i;{
cout<<"sum="<<sum;
}
return 0;
}
